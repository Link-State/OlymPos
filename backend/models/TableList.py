import sys
import os

sys.path.append(os.path.dirname(os.path.abspath(os.path.dirname(__file__))))

from main import connection, command

def getTables(store_uid=-1) :
    # store uid, table num, state, isLogin
    return

def setTableNum(store_uid=-1, num=-1) :
    return

def setState(store_uid=-1, num=-1) :
    return

def setIsLogin(store_uid=-1, num=-1) :
    return

def add(**kwargs) :
    # require : store uid, table num
    return

def remove(store_uid=-1, num=-1) :
    return